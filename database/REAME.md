# Base de datos — RiskGuard

## Requisitos
- PostgreSQL corriendo localmente.
- Extensión `pdo_pgsql` habilitada en PHP (en WAMP: icono de WAMP → PHP → Extensiones PHP → marcar `pdo_pgsql` y `pgsql`, luego reiniciar servicios).

## Pasos para cada integrante del equipo

1. Crear la base de datos local: